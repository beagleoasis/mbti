spring:
  h2:
    console:
      enabled: true

  datasource:
      url: jdbc:h2:mem:testdb

  jpa:
      show-sql: true
      generate-ddl: true
      hibernate:
        ddl-auto: create-drop
      defer-datasource-initialization: true